{
  "name": "zippia",
  "version": "1.0.0",
  "description": "zippia test application",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev-server": "nodemon index.js",
    "server": "node index.js",
    "client": "cd client && yarn start",
    "dev": "concurrently -n 'server,client' -c 'green,blue' \"npm run dev-server\" \"npm run client \"",
    "start": "concurrently -n 'server,client' -c 'green,blue' \"npm run server\" \"npm run client \""
  },
  "author": "taimoor khan",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.21.1",
    "concurrently": "^6.2.0",
    "express": "^4.17.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.12"
  }
}
